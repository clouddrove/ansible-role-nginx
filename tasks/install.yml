---
# install nginx

- name: Install the nginx package
  apt:
    name: nginx
    state: "{{ state }}"
    update_cache: yes

- name: reload systemd
  command: systemctl daemon-reload
  notify:
     - restart nginx
